trigger:
- master

resources:
- repo: self

variables:
  dockerRegistryServiceConnection: '15ddcb32-59fd-4287-aa59-e7c740e5e7c1'
  imageRepository: 'doug'
  containerRegistry: 'dougregistry.azurecr.io'
  dockerfilePath: 'Doug/Dockerfile'
  tag: '$(Build.BuildId)'
  
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build and push stage
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        buildContext: '.'
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
